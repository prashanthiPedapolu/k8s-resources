# cluster.yaml
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop-dev
  region: us-east-1
managedNodeGroups:
  - name: roboshop-dev-ng
    instanceTypes: ["m5.large", "c3.large","c4.large","c5.large"]
    spot: true
    desiredCapacity: 2
    minSize: 1
    maxSize: 4
